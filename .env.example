# Copy this file to .env for local dev, or set this as an
# Environment Variable in your Vercel/Netlify project settings.
#
# URL of the deployed FastAPI backend (no trailing slash).
# Leave unset for local dev — it defaults to http://localhost:8000
VITE_API_URL=https://your-backend.onrender.com
